// Generated by CoffeeScript 1.7.1
(function() {
  var Spotler;

  Spotler = function(baseUrl, newContext) {
    var self;
    self = this;
    this._name = null;
    this._userGrid = null;
    this._fireBase = null;
    this._mainUser = null;
    this._fullName = null;
    this._searchHandler = null;
    this._currentSearch = null;
    this.handlers = [];
    if (!baseUrl || typeof baseUrl !== 'string') {
      throw new Error('Invalid baseUrl provided');
    }
    this._fireBase = new Firebase(baseUrl, newContext || false ? new Firebase.Context() : null);
    this._authHandlers = [];
    this._fireBaseAuthClient = new FirebaseSimpleLogin(this._fireBase, function(error, user) {
      return self._onloginStateChange(error, user);
    });
    return console.log('Oh! Yeah!');
  };

  $('#mouseHandler').on('mouseover', 'div', function(e) {
    event.preventDefault();
    event.stopPropagation();
    return console.log('Survol de ' + $(this).val((function() {
      function _Class() {}

      return _Class;

    })()));
  });

  $('form').on('mouseover', 'button[type="submit"]', function(e) {
    event.preventDefault();
    event.stopPropagation();
    return console.log('Hover form button ok');
  });

  $('div').on('mouseover', 'a[href^="http"]', function(e) {
    event.preventDefault();
    event.stopPropagation();
    $(this).attr({
      target: '_blank'
    });
    return console.log('Hover div link ok');
  });

}).call(this);
